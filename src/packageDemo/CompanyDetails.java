package packageDemo;

public class CompanyDetails {
	//static belongs to the class, so it must be declared directly inside the class, not inside a method.
	public static int x  =10;
	//Inside a method like main(), you can create a local variable, but you cannot use access modifiers or static for that local variable.

}
